
  # Create Carbon Tokenization Landing Page

  This is a code bundle for Create Carbon Tokenization Landing Page. The original project is available at https://www.figma.com/design/MUBDTBP4K46Oy0jBhoCEdZ/Create-Carbon-Tokenization-Landing-Page.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  